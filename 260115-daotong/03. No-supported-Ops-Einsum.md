# 参考链接
https://www.hiascend.com/forum/thread-0230168517548674019-1-1.html

从参考链接的截图报错来看（静态维度，ATC转换能够成功，Op相关Warning相似），精简如下，
```shell
ATC start working now, please wait for a moment.
...
Warning: set_tiling_params does not take effect because get_op_mode is dynamic
...
W11001: [PID: 1070079] 2024-11-28-13:34:03.xxx Op [...] does not hit the high-priority operator information library, which might result in compromised performance.
（此警告重复多次，涉及多个 transformer 层中的 Div 操作）
```

# 软件版本配套降级
torch 版本从 2.7 降级到 1.11 ，torch 相关版本配套（torch_npu, torchvision, cann相关）跟随降级调整，
```shell
torch              1.11.0
torch-npu          1.11.0.post14
torchvision        0.12.0
```
```shell
toolkit_running_version=[7.3.0.1.231:8.0.RC2]
```

python 版本从 3.11 降级到 3.10，pip 相关版本配套降级如下，
```shell
Python 3.10.12
```
```shell
numpy从2.x降级到1.x， 2.x -> 1.24.3
decorator
sympy
scipy
psutil
```

# ATC 转换
按照 **01. custom-core-dump.md** 中的转换方式，
- sam.pt 转 sam.onnx 转 sam.om，出现`op[/trunk/Greater], The /trunk/Greater op dtype is not same, type1:DT_INT32, type2:DT_INT64`，降级之后报错依旧，
```shell
tc --model=/data/fuyuxin/onnx_weights/sam3.onnx \
    --framework=5 \
    --output=/data/fuyuxin/myworkdir/sam3 \
    --soc_version=Ascend910B \
    --input_shape="img_batch:1,3,1008,1008;language_features:32,1,256;language_mask:1,32" \
    --input_format=NCHW \
    --disable_reuse_memory=1 \
    --log=info

ATC start working now, please wait for a moment.
...
ATC run failed, Please check the detail log, Try 'atc --help' for more information
E10042: 2026-01-19-11:19:02.527.402 GenerateOfflineModel execute failed.
        TraceBack (most recent call last):
        op[/trunk/Greater], The /trunk/Greater op dtype is not same, type1:DT_INT32, type2:DT_INT64[FUNC:CheckTwoInputDtypeSame][FILE:util.cc][LINE:115]
        Verifying /trunk/Greater failed.[FUNC:InferShapeAndType][FILE:infershape_pass.cc][LINE:125]
        Call InferShapeAndType for node:/trunk/Greater(Greater) failed[FUNC:Infer][FILE:infershape_pass.cc][LINE:113]
        process pass InferShapePass on node:/trunk/Greater failed, ret:4294967295[FUNC:RunPassesOnNode][FILE:base_pass.cc][LINE:563]
        build graph failed, graph id:0, ret:1343242270[FUNC:BuildModelWithGraphId][FILE:ge_generator.cc][LINE:1608]
        GenerateOfflineModel execute failed.
```

- 脚本修复 Greater 算子数据类型冲突，强制统一 `dtype:DT_INT32`，得到新的 onnx 文件，降级之后报错更新，不再是算子`MatMul`报错，而是缺少算子`Einsum`支持，
```shell
atc --model=/data/fuyuxin/onnx_weights/customer_sam3_fixed.onnx \
    --framework=5 \
    --output=/data/fuyuxin/myworkdir/customer_sam3_fixed \
    --soc_version=Ascend910B \
    --input_shape="img_batch:1,3,1008,1008;language_features:32,1,256;language_mask:1,32" \
    --input_format=NCHW \
    --disable_reuse_memory=1 \
    --log=info

ATC start working now, please wait for a moment.
...
ATC run failed, Please check the detail log, Try 'atc --help' for more information
EZ3003: 2026-01-19-11:21:15.198.196 No supported Ops kernel and engine are found for [/segmentation_head/mask_predictor/Einsum], optype [Einsum].
        Possible Cause: The operator is not supported by the system. Therefore, no hit is found in any operator information library.
        Solution: 1. Check that the OPP component is installed properly. 2. Submit an issue to request for the support of this operator type.
        TraceBack (most recent call last):
        Assert ((SelectEngine(node_ptr, exclude_engines, is_check_support_success, op_info)) == ge::SUCCESS) failed[FUNC:operator()][FILE:engine_place.cc][LINE:144]
        build graph failed, graph id:0, ret:-1[FUNC:BuildModelWithGraphId][FILE:ge_generator.cc][LINE:1608]
        GenerateOfflineModel execute failed.
```

- 重新 pt 转 onnx，保持 onnx 输出头的数量和源码中的保持一致，静态转换方式，避免 Greater 算子出现 dtype 数据类型冲突，onnx转om转换不再成功，失败缺少算子`Einsum`支持，
```shell
atc --model=/data/fuyuxin/onnx_weights/customer_sam3_fixed_int64.onnx \
    --framework=5 \
    --output=/data/fuyuxin/myworkdir/customer_sam3_fixed \
    --soc_version=Ascend910B \
    --input_shape="img_batch:1,3,1008,1008;language_features:32,1,256;language_mask:1,32" \
    --input_format=NCHW \
    --disable_reuse_memory=1 \
    --log=info

ATC start working now, please wait for a moment.
...
ATC run failed, Please check the detail log, Try 'atc --help' for more information
EZ3003: 2026-01-19-11:23:31.360.310 No supported Ops kernel and engine are found for [/segmentation_head/mask_predictor/Einsum], optype [Einsum].
        Possible Cause: The operator is not supported by the system. Therefore, no hit is found in any operator information library.
        Solution: 1. Check that the OPP component is installed properly. 2. Submit an issue to request for the support of this operator type.
        TraceBack (most recent call last):
        Assert ((SelectEngine(node_ptr, exclude_engines, is_check_support_success, op_info)) == ge::SUCCESS) failed[FUNC:operator()][FILE:engine_place.cc][LINE:144]
        build graph failed, graph id:0, ret:-1[FUNC:BuildModelWithGraphId][FILE:ge_generator.cc][LINE:1608]
        GenerateOfflineModel execute failed.
```
